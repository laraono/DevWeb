/*
Use os eventos de click disponíveis para:
    1) Esconder a "propaganda da feira de cursos" da UFPR usando .slideUp()

    2) Mostrar a imagem usando .slideDown()

    3) Mostrar e esconder a sidebar usando .slideToggle()

Por fim, coloque números inteiros, como por exemplo 500, nos parâmetros dos métodos. Atualize a página e teste
os botões.

Curiosidade: Os métodos slideUp(), slideDown(), slideToggle() aceitam um segundo parâmetro que é uma função anônima,
ou seja, quando o slide finalizar, o código da função é executado.
*/

$("#botaoUFPR").click(function() {
    
});

$("#botaoImagem").click(function() {
     
});

$("#botaoSidebar").click(function() {
    
});